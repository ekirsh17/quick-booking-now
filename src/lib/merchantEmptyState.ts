/** Description text under merchant empty-state headings (wider on mobile, xs from sm+). */
export const merchantEmptyStateDescriptionClass =
  "mx-auto max-w-[18rem] text-pretty text-sm text-muted-foreground sm:max-w-xs";

/** Primary CTA under merchant empty states: 44px touch target on mobile, h-9 on desktop (matches header). */
export const merchantEmptyStateActionClass = "min-h-11 lg:h-9 lg:min-h-0";
